---
phase: 01-schema-generation-core-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CanonicaLib.UI/Services/DefaultSchemaGenerator.cs
autonomous: true

must_haves:
  truths:
    - "Schema generation completes for SelfReferencingEntity (with Children array and Parent reference) without stack overflow"
    - "Schema generation completes for Node/Edge circular references producing $ref for both types"
    - "Schema generation completes for TreeNode (left/right self-references) producing $ref"
    - "Custom collection classes inheriting from List<T> are detected as array schemas, not object schemas"
    - "Dictionary<TKey, TValue> types produce object schemas with additionalProperties, not array schemas"
    - "All 11 existing tests continue to pass after changes"
  artifacts:
    - path: "CanonicaLib.UI/Services/DefaultSchemaGenerator.cs"
      provides: "Fixed schema generation with early registration, improved collection detection, dictionary handling"
      contains: "GetInterfaces"
  key_links:
    - from: "DefaultSchemaGenerator.CreateSchemaFromType"
      to: "GeneratorContext.AddSchema"
      via: "Early schema registration before recursive property/element processing"
      pattern: "generatorContext\\.AddSchema.*schema.*referenceType"
    - from: "DefaultSchemaGenerator.IsArrayOrCollection"
      to: "Type.GetInterfaces()"
      via: "Interface-based collection detection"
      pattern: "GetInterfaces.*IEnumerable"
---

<objective>
Fix BUG-01 (infinite recursion on self-referencing models) and BUG-04 (incomplete collection type detection) in DefaultSchemaGenerator.cs.

Purpose: These two bugs cause schema generation to stack overflow on tree structures and misclassify custom collection types. Fixing them makes the schema generator handle all type patterns correctly, which is the foundation for Phase 2 testing.

Output: A corrected DefaultSchemaGenerator.cs where (1) all code paths add schemas to context before recursive processing, (2) collection detection uses GetInterfaces() for reliable IEnumerable<T> checking, and (3) dictionary types produce proper additionalProperties schemas.
</objective>

<execution_context>
@C:\Users\ripza\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\ripza\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-schema-generation-core-fixes/01-RESEARCH.md
@CanonicaLib.UI/Services/DefaultSchemaGenerator.cs
@CanonicaLib.UI/GeneratorContext.cs
@CanonicaLib.UI.Tests/Services/DefaultSchemaGeneratorTests.cs
@CanonicaLib.UI.Tests/TestModels/SelfReferencingEntity.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix collection detection and dictionary handling methods</name>
  <files>CanonicaLib.UI/Services/DefaultSchemaGenerator.cs</files>
  <action>
Fix the private helper methods that detect collection and dictionary types. These changes are prerequisites for the main flow fix in Task 2.

1. **Replace `IsArrayOrCollection` method (lines 288-305)** with an interface-based approach:
   - First check `type.IsArray` (keep existing behavior for arrays)
   - Then check if the type itself is a generic `IEnumerable<>` (for interface types like `IList<string>`)
   - Then use `type.GetInterfaces().Any(i => i.IsGenericType && i.GetGenericTypeDefinition() == typeof(IEnumerable<>))` to detect ALL collection types including custom classes inheriting from `List<T>`, `Collection<T>`, etc.
   - Remove the recursive `BaseType` fallback — `GetInterfaces()` already traverses the inheritance hierarchy automatically
   - Add `using System.Linq;` if not already present (it likely is via global usings)

2. **Replace `IsObjectType` method (lines 224-236)** with a proper `IsDictionary` method:
   - Rename from `IsObjectType` to `IsDictionary` (more precise naming)
   - Check if type is a generic type with `GetGenericTypeDefinition() == typeof(Dictionary<,>)` or `typeof(IDictionary<,>)`
   - Also check `type.GetInterfaces().Any(i => i.IsGenericType && i.GetGenericTypeDefinition() == typeof(IDictionary<,>))` for types inheriting from Dictionary
   - This handles `Dictionary<string, int>`, `IDictionary<string, int>`, and custom classes inheriting from `Dictionary<TKey, TValue>`

3. **Fix `GetElementType` method (lines 307-325)** to handle inherited collections:
   - Keep `type.IsArray` -> `type.GetElementType()` (unchanged)
   - For non-array types, find the `IEnumerable<T>` interface via `GetInterfaces()` and extract T from it
   - This correctly handles `CustomList : List<string>` where `GetGenericArguments()` on the type itself may not return the element type
   - Fall back to existing `GetGenericArguments().FirstOrDefault()` and base type recursion

Important: Do NOT change the `CreateSchemaFromType` method in this task — that's Task 2. Only change the helper methods.
  </action>
  <verify>
Run `dotnet build CanonicaLib.UI` — must compile with no errors. Warnings about nullable references in test project are acceptable.
Run `dotnet test CanonicaLib.UI.Tests` — all 11 existing tests must still pass.
  </verify>
  <done>
`IsArrayOrCollection` uses `GetInterfaces()` to detect all IEnumerable<T> implementations.
`IsDictionary` (renamed from `IsObjectType`) properly detects Dictionary<,> and IDictionary<,> types.
`GetElementType` extracts element type from inherited collection types via interface inspection.
All existing tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix CreateSchemaFromType to prevent infinite recursion and handle dictionaries</name>
  <files>CanonicaLib.UI/Services/DefaultSchemaGenerator.cs</files>
  <action>
Fix the main `CreateSchemaFromType` method to use early schema registration in all code paths and add proper dictionary handling.

1. **Add dictionary handling BEFORE the collection check (before line 103)**:
   - Insert a new block after the `IsObjectType`/`IsDictionary` check (line 95-100) — or replace the existing `IsObjectType` block entirely since we renamed the method to `IsDictionary`:
   - When `IsDictionary(type)` is true:
     - Set `schema.Type = JsonSchemaType.Object`
     - Find the `IDictionary<,>` interface using `type.GetInterfaces().FirstOrDefault(i => i.IsGenericType && i.GetGenericTypeDefinition() == typeof(IDictionary<,>))`
     - If found, extract the value type (index [1] of `GetGenericArguments()`) and set `schema.AdditionalProperties = CreateSchemaFromType(valueType, referenceType, generatorContext)`
     - If the type itself is generic `IDictionary<,>`, use `type.GetGenericArguments()[1]` directly
     - Log a warning if the key type is not string: `_logger.LogWarning("Dictionary key type {KeyType} is not string. OpenAPI only supports string keys.", keyType.FullName)`
     - Return the schema (dictionaries are typically inline, not registered as components, unless they are internal/external reference types — follow the same pattern as enums: try `AddSchema`, return reference if added)
   - This MUST come before the `IsArrayOrCollection` check because dictionaries implement `IEnumerable<KeyValuePair<TKey, TValue>>` and would be incorrectly detected as collections

2. **Fix the array/collection handling block (lines 103-114)** to add early schema registration:
   - After setting `schema.Type = JsonSchemaType.Array` and getting `elementType`, add:
     ```
     var schemaAddedEarly = generatorContext.AddSchema(type, schema, referenceType);
     ```
   - BEFORE calling `CreateSchemaFromType(elementType, ...)` for the items
   - After processing items, if `schemaAddedEarly` is true, return `new OpenApiSchemaReference(GetSchemaKey(type))` instead of the raw schema
   - This prevents infinite recursion when the element type is self-referencing (e.g., `SelfReferencingEntity[]` where `SelfReferencingEntity` has a `Children` property of type `SelfReferencingEntity[]`)

3. **Remove the commented-out `CreateSchemaOrReference` method (lines 196-222)** — dead code cleanup.

4. **Verify the object handling block (lines 142-193)** still works correctly:
   - The early schema registration at line 151 (`var schemaAddedEarly = generatorContext.AddSchema(type, schema, referenceType)`) is already correct
   - No changes needed here — just verify it's intact after your edits

Important: The `IsDictionary` call must replace the old `IsObjectType` call at line 95. Update the method name in the condition.
  </action>
  <verify>
Run `dotnet build CanonicaLib.UI` — must compile with no errors.
Run `dotnet test CanonicaLib.UI.Tests` — all 11 existing tests must still pass.
Verify by reading DefaultSchemaGenerator.cs that:
  - Dictionary check appears BEFORE collection check in CreateSchemaFromType
  - Collection handling has `generatorContext.AddSchema(type, schema, referenceType)` BEFORE `CreateSchemaFromType(elementType, ...)`
  - No commented-out dead code remains
  </verify>
  <done>
Dictionary types are handled before collection types, producing object schemas with additionalProperties.
Array/collection code path registers schemas early before recursing into element types.
All self-referencing models (SelfReferencingEntity, Node/Edge, TreeNode) generate schemas without infinite recursion.
All 11 existing tests pass.
Dead code (commented-out CreateSchemaOrReference) is removed.
  </done>
</task>

</tasks>

<verification>
After both tasks are complete, run the full verification:

1. `dotnet build CanonicaLib.UI` — clean compilation (zero errors)
2. `dotnet test CanonicaLib.UI.Tests` — all 11 tests pass
3. Manual code review of DefaultSchemaGenerator.cs confirming:
   - `IsDictionary` check comes before `IsArrayOrCollection` in `CreateSchemaFromType`
   - Array/collection block has early schema registration (`AddSchema` before recursive `CreateSchemaFromType`)
   - `IsArrayOrCollection` uses `GetInterfaces()` for detection
   - `GetElementType` handles inherited collections via interface inspection
   - No dead code (commented-out methods removed)
   - No `using` statements added that are unused
</verification>

<success_criteria>
1. `dotnet test CanonicaLib.UI.Tests` reports 11 passed, 0 failed
2. DefaultSchemaGenerator.cs compiles cleanly
3. The code changes address BUG-01 (early schema registration in array path) and BUG-04 (interface-based collection detection)
4. Dictionary types are properly handled with additionalProperties
</success_criteria>

<output>
After completion, create `.planning/phases/01-schema-generation-core-fixes/01-01-SUMMARY.md`
</output>
